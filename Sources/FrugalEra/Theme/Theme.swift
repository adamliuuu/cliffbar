import SwiftUI

enum Theme {
    enum colors {
        static let feedColors: [Color] = [
            Color.blue.opacity(0.8),
            Color.purple.opacity(0.8),
            Color.green.opacity(0.8),
            Color.orange.opacity(0.8),
            Color.pink.opacity(0.8)
        ]
    }
} 